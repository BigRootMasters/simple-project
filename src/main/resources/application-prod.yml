#服务端口
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/local?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  application:
    name: simple-spring-boot-project
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml